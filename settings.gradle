pluginManagement {
    repositories {
        mavenCentral() // 必要に応じて他のリポジトリも追加
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositories {
        mavenCentral() // リポジトリを指定する
        gradlePluginPortal()
    }
}


rootProject.name = 'EnemyDownApp'